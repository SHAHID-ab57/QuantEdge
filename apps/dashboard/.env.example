# Public environment variables — inlined by Next.js at build time.
# Copy to .env.local and adjust values when the backend runs elsewhere.
NEXT_PUBLIC_API_URL=http://localhost:8000
NEXT_PUBLIC_WS_URL=wss://public-socket.india.delta.exchange
NEXT_PUBLIC_APP_NAME=Research Dashboard